# 总体架构
- **RK3588S**：图像处理 + 数据管理 + 上位机界面 + 网络通信
- **STM32 / ESP32**：传感器采集 + 实时报警执行


```mermaid 
flowchart TD
    subgraph A [RK3588S开发板]
        direction LR
        A1[应用程序] --> A2[Linux WDT驱动<br>（如 /dev/watchdog）]
        A2 --> A3[硬件看门狗<br>（芯片内部或外部）]
    end

    subgraph B [STM32]
        direction LR
        B1[主循环<br>发送心跳] --> B2[独立看门狗 IWDG<br>（硬件外设）]
    end

    A3 -- 系统复位 --> A
    B2 -- 系统复位 --> B
    
    A1 -- "串口心跳包" --> B1
    B1 -- "串口心跳包" --> A1
    
```


```mermaid
flowchart TD
    %% 定义外部设备（摄像头、烟雾传感器、声光警报）
    
    smoke[烟雾传感器]
    alarm[声光警报]

    %% RK3588S 开发板子图
    subgraph R [RK3588S 开发板]
        direction TB
        camera[摄像头]
        R_A[程序 A<br>YOLO 图像识别]
        R_B[程序 B<br>火灾判断]
        R_daemon[Linux 守护进程]
        R_storage[存储<br>（传感器信息保存）]
    end

    %% STM32 开发板子图
    subgraph S [STM32 开发板]
        direction TB
        S_main[STM32 主程序<br>（传感器读取、警报控制）]
        S_watchdog[硬件看门狗 IWDG]
    end

    %% 连接关系
    camera -- 图像数据 --> R_A
    smoke  -- 烟雾数据 --> S_main

    %% B 调用 A 获取识别结果（双向通信表示调用与返回）
    R_B <--> R_A

    %% STM32 发送烟雾数据给 R 的 B 程序（串口通信）
    S_main -- 烟雾数据（串口） --> R_B

    %% B 综合判断后触发警报并保存数据
    R_B -- 火灾警报触发 --> S_main
    R_B --> R_storage

    %% STM32 控制声光警报
    S_main -- 触发信号 --> alarm

    %% 守护进程监控 R 上的程序 A 和 B
    R_daemon -.-> R_A
    R_daemon -.-> R_B

    %% 看门狗监控 STM32 主程序（喂狗关系）
    S_watchdog -.-> S_main


```

```mermaid
flowchart TB
    %% 外部设备（放在左右两侧）
    camera[摄像头] --> R_A
    smoke[烟雾传感器] --> S_main
    
    subgraph R [RK3588S 开发板]
        direction TB
        R_A[程序 A<br>YOLO 图像识别]
        R_B[程序 B<br>火灾判断]
        R_daemon[Linux 守护进程]
        R_storage[存储<br>（传感器信息保存）]

        R_B --> R_storage
        R_daemon -.-> R_A
        R_daemon -.-> R_B
    end

    subgraph S [STM32 开发板]
        direction TB
        S_main[STM32 主程序<br>（传感器读取、警报控制）]
        S_watchdog[硬件看门狗 IWDG]

        S_watchdog -.-> S_main
    end

    %% 开发板间通信
    R_B <--> S_main  
    S_main --> alarm[声光警报<br>（两个硬件）]

    %% 注释：如果需要更明确的烟雾数据流向，可以用下面这行代替上面的双向箭头中的部分
    %% S_main -- 烟雾数据 --> R_B
```

## 1️⃣ RK3588S（Ubuntu）—— 核心计算节点 ⭐⭐⭐⭐⭐

**它负责什么：**
- 摄像头接入（USB / MIPI）
- 火焰 + 烟雾图像算法
- 实时画面显示
- 报警事件管理
- 历史数据存储
- 网络通信（以太网 / WiFi）
**为什么选它：**
- 算力强（ARM A76 + GPU / NPU）
- 跑 Ubuntu，开发效率高
- “图像处理” + “系统软件”味道很正

📌 论文里可以写：

> The RK3588S platform is used as the main processing unit to perform image processing and data management tasks.

---
## 2️⃣ STM32 / ESP32 —— 前端采集与控制节点 ⭐⭐⭐⭐
### 推荐：**STM32 优先，ESP32 备用**
**负责什么：**
- 烟雾传感器 ADC 采集
- 阈值判断（第一层保护）
- 声光报警控制
- 与 RK3588S 通信

**为什么要它：**

- 实时性强
- 稳定
- Linux 挂了它还能报警（这是答辩金句）

📌 论文表述：

> A microcontroller is used as the front-end node to ensure real-time sensor acquisition and alarm control.